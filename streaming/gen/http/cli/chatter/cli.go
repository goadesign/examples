// Code generated by goa v3.22.2, DO NOT EDIT.
//
// chatter HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/examples/streaming/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	chatterc "goa.design/examples/streaming/gen/http/chatter/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"chatter (login|echoer|listener|summary|subscribe|history)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` chatter login --user "username" --password "password"` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
	dialer goahttp.Dialer,
	chatterConfigurer *chatterc.ConnConfigurer,
) (goa.Endpoint, any, error) {
	var (
		chatterFlags = flag.NewFlagSet("chatter", flag.ContinueOnError)

		chatterLoginFlags        = flag.NewFlagSet("login", flag.ExitOnError)
		chatterLoginUserFlag     = chatterLoginFlags.String("user", "REQUIRED", "")
		chatterLoginPasswordFlag = chatterLoginFlags.String("password", "REQUIRED", "")

		chatterEchoerFlags     = flag.NewFlagSet("echoer", flag.ExitOnError)
		chatterEchoerTokenFlag = chatterEchoerFlags.String("token", "REQUIRED", "")

		chatterListenerFlags     = flag.NewFlagSet("listener", flag.ExitOnError)
		chatterListenerTokenFlag = chatterListenerFlags.String("token", "REQUIRED", "")

		chatterSummaryFlags     = flag.NewFlagSet("summary", flag.ExitOnError)
		chatterSummaryTokenFlag = chatterSummaryFlags.String("token", "REQUIRED", "")

		chatterSubscribeFlags     = flag.NewFlagSet("subscribe", flag.ExitOnError)
		chatterSubscribeTokenFlag = chatterSubscribeFlags.String("token", "REQUIRED", "")

		chatterHistoryFlags     = flag.NewFlagSet("history", flag.ExitOnError)
		chatterHistoryViewFlag  = chatterHistoryFlags.String("view", "", "")
		chatterHistoryTokenFlag = chatterHistoryFlags.String("token", "REQUIRED", "")
	)
	chatterFlags.Usage = chatterUsage
	chatterLoginFlags.Usage = chatterLoginUsage
	chatterEchoerFlags.Usage = chatterEchoerUsage
	chatterListenerFlags.Usage = chatterListenerUsage
	chatterSummaryFlags.Usage = chatterSummaryUsage
	chatterSubscribeFlags.Usage = chatterSubscribeUsage
	chatterHistoryFlags.Usage = chatterHistoryUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "chatter":
			svcf = chatterFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "chatter":
			switch epn {
			case "login":
				epf = chatterLoginFlags

			case "echoer":
				epf = chatterEchoerFlags

			case "listener":
				epf = chatterListenerFlags

			case "summary":
				epf = chatterSummaryFlags

			case "subscribe":
				epf = chatterSubscribeFlags

			case "history":
				epf = chatterHistoryFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "chatter":
			c := chatterc.NewClient(scheme, host, doer, enc, dec, restore, dialer, chatterConfigurer)
			switch epn {
			case "login":
				endpoint = c.Login()
				data, err = chatterc.BuildLoginPayload(*chatterLoginUserFlag, *chatterLoginPasswordFlag)
			case "echoer":
				endpoint = c.Echoer()
				data, err = chatterc.BuildEchoerPayload(*chatterEchoerTokenFlag)
			case "listener":
				endpoint = c.Listener()
				data, err = chatterc.BuildListenerPayload(*chatterListenerTokenFlag)
			case "summary":
				endpoint = c.Summary()
				data, err = chatterc.BuildSummaryPayload(*chatterSummaryTokenFlag)
			case "subscribe":
				endpoint = c.Subscribe()
				data, err = chatterc.BuildSubscribePayload(*chatterSubscribeTokenFlag)
			case "history":
				endpoint = c.History()
				data, err = chatterc.BuildHistoryPayload(*chatterHistoryViewFlag, *chatterHistoryTokenFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// chatterUsage displays the usage of the chatter command and its subcommands.
func chatterUsage() {
	fmt.Fprintln(os.Stderr, `The chatter service implements a simple client and server chat.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] chatter COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    login: Creates a valid JWT token for auth to chat.`)
	fmt.Fprintln(os.Stderr, `    echoer: Echoes the message sent by the client.`)
	fmt.Fprintln(os.Stderr, `    listener: Listens to the messages sent by the client.`)
	fmt.Fprintln(os.Stderr, `    summary: Summarizes the chat messages sent by the client.`)
	fmt.Fprintln(os.Stderr, `    subscribe: Subscribe to events sent when new chat messages are added.`)
	fmt.Fprintln(os.Stderr, `    history: Returns the chat messages sent to the server.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s chatter COMMAND --help\n", os.Args[0])
}
func chatterLoginUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] chatter login", os.Args[0])
	fmt.Fprint(os.Stderr, " -user STRING")
	fmt.Fprint(os.Stderr, " -password STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Creates a valid JWT token for auth to chat.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -user STRING: `)
	fmt.Fprintln(os.Stderr, `    -password STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `chatter login --user "username" --password "password"`)
}

func chatterEchoerUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] chatter echoer", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Echoes the message sent by the client.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `chatter echoer --token "Sit rerum sit et hic."`)
}

func chatterListenerUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] chatter listener", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Listens to the messages sent by the client.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `chatter listener --token "Aut sunt fugit exercitationem."`)
}

func chatterSummaryUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] chatter summary", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Summarizes the chat messages sent by the client.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `chatter summary --token "Quas soluta rerum voluptatum."`)
}

func chatterSubscribeUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] chatter subscribe", os.Args[0])
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Subscribe to events sent when new chat messages are added.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `chatter subscribe --token "Libero ullam."`)
}

func chatterHistoryUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] chatter history", os.Args[0])
	fmt.Fprint(os.Stderr, " -view STRING")
	fmt.Fprint(os.Stderr, " -token STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Returns the chat messages sent to the server.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -view STRING: `)
	fmt.Fprintln(os.Stderr, `    -token STRING: `)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `chatter history --view "Rerum hic fugit commodi laudantium." --token "Aut tempore quae minus ducimus consequatur."`)
}
