// Code generated by goa, DO NOT EDIT.
//
// interceptors HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/examples/interceptors/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	interceptorsc "goa.design/examples/interceptors/gen/http/interceptors/client"
	interceptors "goa.design/examples/interceptors/gen/interceptors"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"interceptors (get|create|stream)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "interceptors get --body '{\n      \"spanID\": \"c2bc78f1-a675-44df-b132-1d285e204cf2\",\n      \"traceID\": \"8909bcf0-fa7e-4ed1-bc3e-1ed689cc9896\"\n   }' --tenant-id \"0be14847-a562-4081-b76a-5d1ece3ec1c1\" --record-id \"ca7f30e3-8945-4edd-bcb7-7cb57ca63316\" --auth \"Beatae eum.\"" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
	dialer goahttp.Dialer,
	interceptorsConfigurer *interceptorsc.ConnConfigurer,
	interceptorsInter interceptors.ClientInterceptors,
) (goa.Endpoint, any, error) {
	var (
		interceptorsFlags = flag.NewFlagSet("interceptors", flag.ContinueOnError)

		interceptorsGetFlags        = flag.NewFlagSet("get", flag.ExitOnError)
		interceptorsGetBodyFlag     = interceptorsGetFlags.String("body", "REQUIRED", "")
		interceptorsGetTenantIDFlag = interceptorsGetFlags.String("tenant-id", "REQUIRED", "Tenant ID for the request")
		interceptorsGetRecordIDFlag = interceptorsGetFlags.String("record-id", "REQUIRED", "ID of the record to retrieve")
		interceptorsGetAuthFlag     = interceptorsGetFlags.String("auth", "REQUIRED", "")

		interceptorsCreateFlags        = flag.NewFlagSet("create", flag.ExitOnError)
		interceptorsCreateBodyFlag     = interceptorsCreateFlags.String("body", "REQUIRED", "")
		interceptorsCreateTenantIDFlag = interceptorsCreateFlags.String("tenant-id", "REQUIRED", "Tenant ID for the request")
		interceptorsCreateAuthFlag     = interceptorsCreateFlags.String("auth", "REQUIRED", "")

		interceptorsStreamFlags        = flag.NewFlagSet("stream", flag.ExitOnError)
		interceptorsStreamTenantIDFlag = interceptorsStreamFlags.String("tenant-id", "REQUIRED", "Tenant ID for the request")
		interceptorsStreamAuthFlag     = interceptorsStreamFlags.String("auth", "REQUIRED", "")
	)
	interceptorsFlags.Usage = interceptorsUsage
	interceptorsGetFlags.Usage = interceptorsGetUsage
	interceptorsCreateFlags.Usage = interceptorsCreateUsage
	interceptorsStreamFlags.Usage = interceptorsStreamUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "interceptors":
			svcf = interceptorsFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "interceptors":
			switch epn {
			case "get":
				epf = interceptorsGetFlags

			case "create":
				epf = interceptorsCreateFlags

			case "stream":
				epf = interceptorsStreamFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "interceptors":
			c := interceptorsc.NewClient(scheme, host, doer, enc, dec, restore, dialer, interceptorsConfigurer)
			switch epn {
			case "get":
				endpoint = c.Get()
				endpoint = interceptors.WrapGetClientEndpoint(endpoint, interceptorsInter)
				data, err = interceptorsc.BuildGetPayload(*interceptorsGetBodyFlag, *interceptorsGetTenantIDFlag, *interceptorsGetRecordIDFlag, *interceptorsGetAuthFlag)
			case "create":
				endpoint = c.Create()
				endpoint = interceptors.WrapCreateClientEndpoint(endpoint, interceptorsInter)
				data, err = interceptorsc.BuildCreatePayload(*interceptorsCreateBodyFlag, *interceptorsCreateTenantIDFlag, *interceptorsCreateAuthFlag)
			case "stream":
				endpoint = c.Stream()
				endpoint = interceptors.WrapStreamClientEndpoint(endpoint, interceptorsInter)
				data, err = interceptorsc.BuildStreamPayload(*interceptorsStreamTenantIDFlag, *interceptorsStreamAuthFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// interceptorsUsage displays the usage of the interceptors command and its
// subcommands.
func interceptorsUsage() {
	fmt.Fprintln(os.Stderr, `The interceptors service demonstrates a comprehensive use of interceptors combining
	authentication, tenant validation, caching, audit logging, and retry mechanisms. It showcases
	both client-side and server-side interceptors working together to provide a robust service.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] interceptors COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    get: Get retrieves a record by ID with all interceptors in action`)
	fmt.Fprintln(os.Stderr, `    create: Create a new record with all interceptors in action`)
	fmt.Fprintln(os.Stderr, `    stream: Stream records`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s interceptors COMMAND --help\n", os.Args[0])
}
func interceptorsGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] interceptors get", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -tenant-id STRING")
	fmt.Fprint(os.Stderr, " -record-id STRING")
	fmt.Fprint(os.Stderr, " -auth STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get retrieves a record by ID with all interceptors in action`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -tenant-id STRING: Tenant ID for the request`)
	fmt.Fprintln(os.Stderr, `    -record-id STRING: ID of the record to retrieve`)
	fmt.Fprintln(os.Stderr, `    -auth STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "interceptors get --body '{\n      \"spanID\": \"c2bc78f1-a675-44df-b132-1d285e204cf2\",\n      \"traceID\": \"8909bcf0-fa7e-4ed1-bc3e-1ed689cc9896\"\n   }' --tenant-id \"0be14847-a562-4081-b76a-5d1ece3ec1c1\" --record-id \"ca7f30e3-8945-4edd-bcb7-7cb57ca63316\" --auth \"Beatae eum.\"")
}

func interceptorsCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] interceptors create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -tenant-id STRING")
	fmt.Fprint(os.Stderr, " -auth STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create a new record with all interceptors in action`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -tenant-id STRING: Tenant ID for the request`)
	fmt.Fprintln(os.Stderr, `    -auth STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "interceptors create --body '{\n      \"spanID\": \"7e046fe3-fa36-4f0f-b281-f8c9e1fdf010\",\n      \"traceID\": \"831384a5-5a7d-43cc-94e4-5ec22455f386\",\n      \"value\": \"Impedit voluptatem culpa et.\"\n   }' --tenant-id \"36e02fa8-99a9-4a74-8184-5b23fc179edd\" --auth \"Voluptatum necessitatibus.\"")
}

func interceptorsStreamUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] interceptors stream", os.Args[0])
	fmt.Fprint(os.Stderr, " -tenant-id STRING")
	fmt.Fprint(os.Stderr, " -auth STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Stream records`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -tenant-id STRING: Tenant ID for the request`)
	fmt.Fprintln(os.Stderr, `    -auth STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "interceptors stream --tenant-id \"2a36e0ce-eee4-4dc0-a9e8-3770a62bc0e1\" --auth \"Aliquid et dignissimos delectus facere nesciunt.\"")
}
