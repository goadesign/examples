// Code generated by goa v3.23.3, DO NOT EDIT.
//
// text HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/examples/encodings/text/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	textc "goa.design/examples/encodings/text/gen/http/text/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"text (concatstrings|concatbytes|concatstringfield|concatbytesfield)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "text concatstrings --a \"Et nulla quae nesciunt nemo.\" --b \"Molestiae quis aut sit modi deserunt libero.\"" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		textFlags = flag.NewFlagSet("text", flag.ContinueOnError)

		textConcatstringsFlags = flag.NewFlagSet("concatstrings", flag.ExitOnError)
		textConcatstringsAFlag = textConcatstringsFlags.String("a", "REQUIRED", "Left operand")
		textConcatstringsBFlag = textConcatstringsFlags.String("b", "REQUIRED", "Right operand")

		textConcatbytesFlags = flag.NewFlagSet("concatbytes", flag.ExitOnError)
		textConcatbytesAFlag = textConcatbytesFlags.String("a", "REQUIRED", "Left operand")
		textConcatbytesBFlag = textConcatbytesFlags.String("b", "REQUIRED", "Right operand")

		textConcatstringfieldFlags = flag.NewFlagSet("concatstringfield", flag.ExitOnError)
		textConcatstringfieldAFlag = textConcatstringfieldFlags.String("a", "REQUIRED", "Left operand")
		textConcatstringfieldBFlag = textConcatstringfieldFlags.String("b", "REQUIRED", "Right operand")

		textConcatbytesfieldFlags = flag.NewFlagSet("concatbytesfield", flag.ExitOnError)
		textConcatbytesfieldAFlag = textConcatbytesfieldFlags.String("a", "REQUIRED", "Left operand")
		textConcatbytesfieldBFlag = textConcatbytesfieldFlags.String("b", "REQUIRED", "Right operand")
	)
	textFlags.Usage = textUsage
	textConcatstringsFlags.Usage = textConcatstringsUsage
	textConcatbytesFlags.Usage = textConcatbytesUsage
	textConcatstringfieldFlags.Usage = textConcatstringfieldUsage
	textConcatbytesfieldFlags.Usage = textConcatbytesfieldUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "text":
			svcf = textFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "text":
			switch epn {
			case "concatstrings":
				epf = textConcatstringsFlags

			case "concatbytes":
				epf = textConcatbytesFlags

			case "concatstringfield":
				epf = textConcatstringfieldFlags

			case "concatbytesfield":
				epf = textConcatbytesfieldFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "text":
			c := textc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "concatstrings":
				endpoint = c.Concatstrings()
				data, err = textc.BuildConcatstringsPayload(*textConcatstringsAFlag, *textConcatstringsBFlag)
			case "concatbytes":
				endpoint = c.Concatbytes()
				data, err = textc.BuildConcatbytesPayload(*textConcatbytesAFlag, *textConcatbytesBFlag)
			case "concatstringfield":
				endpoint = c.Concatstringfield()
				data, err = textc.BuildConcatstringfieldPayload(*textConcatstringfieldAFlag, *textConcatstringfieldBFlag)
			case "concatbytesfield":
				endpoint = c.Concatbytesfield()
				data, err = textc.BuildConcatbytesfieldPayload(*textConcatbytesfieldAFlag, *textConcatbytesfieldBFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// textUsage displays the usage of the text command and its subcommands.
func textUsage() {
	fmt.Fprintln(os.Stderr, `The text service performs operations on strings`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] text COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    concatstrings: Concatstrings implements concatstrings.`)
	fmt.Fprintln(os.Stderr, `    concatbytes: Concatbytes implements concatbytes.`)
	fmt.Fprintln(os.Stderr, `    concatstringfield: Concatstringfield implements concatstringfield.`)
	fmt.Fprintln(os.Stderr, `    concatbytesfield: Concatbytesfield implements concatbytesfield.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s text COMMAND --help\n", os.Args[0])
}
func textConcatstringsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] text concatstrings", os.Args[0])
	fmt.Fprint(os.Stderr, " -a STRING")
	fmt.Fprint(os.Stderr, " -b STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Concatstrings implements concatstrings.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -a STRING: Left operand`)
	fmt.Fprintln(os.Stderr, `    -b STRING: Right operand`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "text concatstrings --a \"Et nulla quae nesciunt nemo.\" --b \"Molestiae quis aut sit modi deserunt libero.\"")
}

func textConcatbytesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] text concatbytes", os.Args[0])
	fmt.Fprint(os.Stderr, " -a STRING")
	fmt.Fprint(os.Stderr, " -b STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Concatbytes implements concatbytes.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -a STRING: Left operand`)
	fmt.Fprintln(os.Stderr, `    -b STRING: Right operand`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "text concatbytes --a \"Necessitatibus ipsum officiis illum aut.\" --b \"Tempore aut amet rerum consectetur.\"")
}

func textConcatstringfieldUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] text concatstringfield", os.Args[0])
	fmt.Fprint(os.Stderr, " -a STRING")
	fmt.Fprint(os.Stderr, " -b STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Concatstringfield implements concatstringfield.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -a STRING: Left operand`)
	fmt.Fprintln(os.Stderr, `    -b STRING: Right operand`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "text concatstringfield --a \"Eos accusantium deleniti inventore dolorum maxime.\" --b \"Voluptas optio modi asperiores nesciunt eligendi.\"")
}

func textConcatbytesfieldUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] text concatbytesfield", os.Args[0])
	fmt.Fprint(os.Stderr, " -a STRING")
	fmt.Fprint(os.Stderr, " -b STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Concatbytesfield implements concatbytesfield.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -a STRING: Left operand`)
	fmt.Fprintln(os.Stderr, `    -b STRING: Right operand`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "text concatbytesfield --a \"Inventore nobis officiis quasi iusto.\" --b \"Laudantium quia consectetur quisquam maiores.\"")
}
