// Code generated by goa, DO NOT EDIT.
//
// retry gRPC client CLI support package
//
// Command:
// $ goa gen goa.design/examples/retry/design

package cli

import (
	"flag"
	"fmt"
	"os"

	retryc "goa.design/examples/retry/gen/grpc/retry/client"
	goa "goa.design/goa/v3/pkg"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"retry get-message",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "retry get-message --message '{\n      \"id\": \"Dolores asperiores ipsam.\"\n   }'" + "\n" +
		""
}

// cliStringFlag keeps an omitted command-line flag distinct from an explicitly empty flag.
type cliStringFlag struct {
	value *string
}

// String returns the flag text shown by the standard flag package.
func (f *cliStringFlag) String() string {
	if f.value == nil {
		return ""
	}
	return *f.value
}

// Set records that the user supplied the flag, even when value is empty.
func (f *cliStringFlag) Set(value string) error {
	f.value = &value
	return nil
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	cc *grpc.ClientConn,
	opts ...grpc.CallOption,
) (goa.Endpoint, any, error) {
	var (
		retryFlags = flag.NewFlagSet("retry", flag.ContinueOnError)

		retryGetMessageFlags       = flag.NewFlagSet("get-message", flag.ExitOnError)
		retryGetMessageMessageFlag = new(cliStringFlag)
	)
	retryGetMessageFlags.Var(retryGetMessageMessageFlag, "message", "")

	retryFlags.Usage = retryUsage
	retryGetMessageFlags.Usage = retryGetMessageUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "retry":
			svcf = retryFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "retry":
			switch epn {
			case "get-message":
				epf = retryGetMessageFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "retry":
			c := retryc.NewClient(cc, opts...)
			switch epn {
			case "get-message":
				endpoint = c.GetMessage()
				data, err = retryc.BuildGetMessagePayload(retryGetMessageMessageFlag.value)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// retryUsage displays the usage of the retry command and its subcommands.
func retryUsage() {
	fmt.Fprintln(os.Stderr, `Returns a message after demonstrating one safe retry.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] retry COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    get-message: Returns a stable message after a temporary first-attempt failure.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s retry COMMAND --help\n", os.Args[0])
}
func retryGetMessageUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] retry get-message", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Returns a stable message after a temporary first-attempt failure.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "retry get-message --message '{\n      \"id\": \"Dolores asperiores ipsam.\"\n   }'")
}
