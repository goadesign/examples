// Code generated by goa v3.22.6, DO NOT EDIT.
//
// session HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/examples/cookies/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	sessionc "goa.design/examples/cookies/gen/http/session/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"session (create-session|use-session)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "session create-session --body '{\n      \"name\": \"Ut quas sit at illum tempore ad.\"\n   }'" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		sessionFlags = flag.NewFlagSet("session", flag.ContinueOnError)

		sessionCreateSessionFlags    = flag.NewFlagSet("create-session", flag.ExitOnError)
		sessionCreateSessionBodyFlag = sessionCreateSessionFlags.String("body", "REQUIRED", "")

		sessionUseSessionFlags         = flag.NewFlagSet("use-session", flag.ExitOnError)
		sessionUseSessionSessionIDFlag = sessionUseSessionFlags.String("session-id", "REQUIRED", "")
	)
	sessionFlags.Usage = sessionUsage
	sessionCreateSessionFlags.Usage = sessionCreateSessionUsage
	sessionUseSessionFlags.Usage = sessionUseSessionUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "session":
			svcf = sessionFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "session":
			switch epn {
			case "create-session":
				epf = sessionCreateSessionFlags

			case "use-session":
				epf = sessionUseSessionFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "session":
			c := sessionc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "create-session":
				endpoint = c.CreateSession()
				data, err = sessionc.BuildCreateSessionPayload(*sessionCreateSessionBodyFlag)
			case "use-session":
				endpoint = c.UseSession()
				data, err = sessionc.BuildUseSessionPayload(*sessionUseSessionSessionIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// sessionUsage displays the usage of the session command and its subcommands.
func sessionUsage() {
	fmt.Fprintln(os.Stderr, `The session service illustrates managing user sessions with cookies.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] session COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    create-session: CreateSession implements create_session.`)
	fmt.Fprintln(os.Stderr, `    use-session: UseSession implements use_session.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s session COMMAND --help\n", os.Args[0])
}
func sessionCreateSessionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] session create-session", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `CreateSession implements create_session.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "session create-session --body '{\n      \"name\": \"Ut quas sit at illum tempore ad.\"\n   }'")
}

func sessionUseSessionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] session use-session", os.Args[0])
	fmt.Fprint(os.Stderr, " -session-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `UseSession implements use_session.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -session-id STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "session use-session --session-id \"Qui voluptatem dolores.\"")
}
